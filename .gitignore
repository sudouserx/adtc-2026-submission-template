# Model weights — never commit these to git
model/
model/*.gguf
model/*.bin
model/*.safetensors

# macOS
.DS_Store

# Python
__pycache__/
*.pyc
.venv/

# Local profiler output
submission.json
audit.json

# Local reference (not part of submission)
Gate 2 (Semifinalist Round) Submission Guidelines.pdf

provenance/adapter-dpo/adapter_model.safetensors
provenance/adapter-sft/adapter_model.safetensors
